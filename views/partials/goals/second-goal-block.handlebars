<li>
  <div class="col s12 m6 l4">
    <div class="card blue-grey darken-1">

      <div class="card-content white-text">
        <span class="card-title" id='{{{id}}}' data-id="{{{id}}}" data-parent="{{{dataValues.name}}}">
          <p>{{{dataValues.name}}}</p>
        </span>
      </div>

      <div class="container">
        <h5>Comments:</h5>
        <ul>
            {{#each Tasks}}
              {{> tasks/task-block}}
            {{/each}}
        </ul>
            {{!-- <ul>
          <li>
            {{#each 'Tasks.comments'}}
          </li>
          <ul> --}}


      </div>
      <div class="card-action">
        <a href="#">This is a link</a>
        <a href="#">This is a link</a>
      </div>

    </div>
  </div>
</li>