{{!-- <li> --}}
	{{!-- {{dataValues.goalName}} --}}


{{!-- =============== Colins Work Station =========== --}}

	{{!-- Create the id of the subgoal to equal the id of main goal --}}

{{!-- <a class="waves-effect waves-light btn subGoal" id='{{dataValues.id}}' data-id="{{dataValues.id}}" data-parent="{{dataValues.goalName}}">{{dataValues.goalName}}</a> --}}


    {{!-- <div class="col s12 m6 l4">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text"> 
		      <span class="card-title" id='{{dataValues.id}}' data-id="{{dataValues.id}}" data-parent="{{dataValues.goalName}}">{{dataValues.goalName}}</span>
        </div>
          <div class="card-action">
            <a href="#">This is a link</a>
            <a href="#">This is a link</a>
          </div>
          <ul>
            {{#each subGoals}}
              {{> subGoals/subGoal-block}}
            {{/each}}
          </ul>
          
      </div>
    </div> --}}

	
{{!-- =============== Colins Work Station =========== --}}

{{!-- </li> --}}

{{!-- =============== Sam's Work Station =========== --}}

<li>
  <div class="col s12 m6 l4">
      <div class="card small blue-grey darken-1">

        <div class="card-content white-text"> 
		      <div class="container peter">
            <div class="row">
              <span class="card-title center" id='{{{dataValues.id}}}' data-id="{{{dataValues.id}}}" data-parent="{{{dataValues.name}}}">
            <button class="goal-card-button robert waves-effect waves-purple btn-large" id='{{{dataValues.id}}}'>{{{dataValues.name}}}</button>
          </span>
            </div>
          </div>
        </div>

          {{!-- <div class="card-action">
            <a href="#">This is a link</a>
            <a href="#">This is a link</a>
          </div> --}}
          <div class="container fred">
            <div class="mary valign-wrapper">
          <h5 class="fred blue-grey-text text-lighten-4 ">Sub Goals:
            {{!-- <button class="fred col s1 add-subgoal-button btn-floating btn-small waves-effect waves-light red" data-reference-goal-id="{{{dataValues.id}}}"><i class="material-icons">add</i></button> --}}
          </h5>
          <button class="fred add-subgoal-button btn-floating btn-small waves-effect waves-light red" data-reference-goal-id="{{{dataValues.id}}}"><i class="material-icons">add</i></button>
          </div>
          <ul class="blue-text text-lighten-5 collection-header">
            {{#each Subgoals}}
              {{> subGoals/subGoal-block}}
            {{/each}}
          </ul>
          </div>

          
          
      </div>
    </div>
</li>